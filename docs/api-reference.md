# API Reference

Full interactive API documentation is auto-generated by FastAPI and available at runtime:

- **Swagger UI**: `http://localhost:8000/docs`
- **ReDoc**: `http://localhost:8000/redoc`
- **OpenAPI JSON**: `http://localhost:8000/openapi.json`

---

## Endpoint Overview

### Auth (`/api/v1/auth`)

| Method | Path | Auth | Description |
|--------|------|------|-------------|
| `POST` | `/api/v1/auth/login` | None | Login with email/password, returns JWT tokens |
| `POST` | `/api/v1/auth/refresh` | Refresh cookie | Issue new access token |
| `POST` | `/api/v1/auth/logout` | Bearer | Invalidate refresh token |

### Users (`/api/v1/users`)

| Method | Path | Auth | Role | Description |
|--------|------|------|------|-------------|
| `GET` | `/api/v1/users/me` | Bearer | any | Get current user profile |
| `PUT` | `/api/v1/users/me` | Bearer | any | Update own profile |
| `GET` | `/api/v1/users` | Bearer | admin | List all users |
| `POST` | `/api/v1/users` | Bearer | admin | Create a new user |
| `PUT` | `/api/v1/users/{id}` | Bearer | admin | Update user |
| `DELETE` | `/api/v1/users/{id}` | Bearer | admin | Delete user |

### Devices (`/api/v1/devices`)

| Method | Path | Auth | Role | Description |
|--------|------|------|------|-------------|
| `GET` | `/api/v1/devices` | Bearer | viewer | List all known devices |
| `POST` | `/api/v1/devices/discover` | Bearer | admin | Trigger device discovery |
| `GET` | `/api/v1/devices/{ain}` | Bearer | viewer | Get device details + state |
| `POST` | `/api/v1/devices/{ain}/on` | Bearer | user | Turn device on |
| `POST` | `/api/v1/devices/{ain}/off` | Bearer | user | Turn device off |
| `PUT` | `/api/v1/devices/{ain}/temperature` | Bearer | user | Set thermostat temperature |
| `PUT` | `/api/v1/devices/{ain}/brightness` | Bearer | user | Set dimmer brightness |
| `PATCH` | `/api/v1/devices/{ain}` | Bearer | user | Update device label/location |

### Schedules (`/api/v1/schedules`)

| Method | Path | Auth | Role | Description |
|--------|------|------|------|-------------|
| `GET` | `/api/v1/schedules` | Bearer | viewer | List all schedules |
| `POST` | `/api/v1/schedules` | Bearer | user | Create a schedule |
| `GET` | `/api/v1/schedules/{id}` | Bearer | viewer | Get schedule details |
| `PUT` | `/api/v1/schedules/{id}` | Bearer | user | Update schedule |
| `DELETE` | `/api/v1/schedules/{id}` | Bearer | user | Delete schedule |

### Automations (`/api/v1/automations`)

| Method | Path | Auth | Role | Description |
|--------|------|------|------|-------------|
| `GET` | `/api/v1/automations` | Bearer | viewer | List all automation rules |
| `POST` | `/api/v1/automations` | Bearer | user | Create automation rule |
| `PUT` | `/api/v1/automations/{id}` | Bearer | user | Update rule |
| `DELETE` | `/api/v1/automations/{id}` | Bearer | user | Delete rule |

### Weather (`/api/v1/weather`)

| Method | Path | Auth | Role | Description |
|--------|------|------|------|-------------|
| `GET` | `/api/v1/weather/current` | Bearer | viewer | Get cached current weather |

### Audit (`/api/v1/audit`)

| Method | Path | Auth | Role | Description |
|--------|------|------|------|-------------|
| `GET` | `/api/v1/audit` | Bearer | admin | List audit events (paginated, filterable) |

### System

| Method | Path | Auth | Description |
|--------|------|------|-------------|
| `GET` | `/health` | None | Health check |
| `GET` | `/` | Bearer | Dashboard (HTML) |

---

## Authentication

All protected endpoints require a JWT access token in the `Authorization` header:

```
Authorization: Bearer <access_token>
```

Obtain a token via `POST /api/v1/auth/login`:

```json
{
  "email": "admin@example.com",
  "password": "yourpassword"
}
```

Response:
```json
{
  "access_token": "eyJ...",
  "token_type": "bearer",
  "expires_in": 900
}
```

The refresh token is returned as an `httponly` cookie and automatically sent on `POST /api/v1/auth/refresh`.

---

## Error Responses

All errors follow a consistent format:

```json
{
  "detail": "Human-readable error message",
  "code": "machine_readable_error_code"
}
```

Common HTTP status codes:

| Code | Meaning |
|------|---------|
| 400 | Invalid request parameters |
| 401 | Missing or invalid authentication |
| 403 | Insufficient role/permissions |
| 404 | Resource not found |
| 422 | Validation error (Pydantic) |
| 503 | FRITZ!Box provider unavailable |
